## Project Overview

This project was developed as an introductory Python graphics assignment using Turtle. It focuses on converting simple character-based color codes into visual pixel-art drawings. Each character represents a specific color, and the program uses Turtle to draw filled square pixels on the canvas.

The program supports three main modes: drawing from a custom color-code string, generating a checkerboard pattern, and rendering predefined characters from text files.
